#pragma once

const char* ssid = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";

// Optional firmware overrides (defaults used if not defined)
// #define WIFI_MAX_ATTEMPTS 40
// #define NTP_SERVER "pool.ntp.org"
// #define NTP_OFFSET 0
// #define NTP_INTERVAL 3600000
// #define OTA_PASSWORD "my-ota-password"
